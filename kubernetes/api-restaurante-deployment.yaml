apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-restaurante-deployment
spec:
  template:
    metadata:
      name: api-restaurante
      labels:
        app: api-restaurante
    spec:
      containers:
        - name: api-restaurante-container
          image: gabrielumbelino/restaurante-api:1.0
          ports:
            - containerPort: 80
          envFrom:
            - configMapRef:
                name: api-restaurante-configmap
          livenessProbe:
            httpGet:
              path: /api
              port: 80
            periodSeconds: 10
            failureThreshold: 3
            initialDelaySeconds: 20
          readinessProbe:
            httpGet:
              path: /api
              port: 80
            periodSeconds: 10
            failureThreshold: 5
            initialDelaySeconds: 3
          resources:
            requests:
              cpu: 100m
  replicas: 1
  selector:
    matchLabels:
      app: api-restaurante
